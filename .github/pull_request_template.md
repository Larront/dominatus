<!--
Feature PRs target `next`. Release PRs (`next` → `main`) deploy: bump the version
in package.json, then after merge tag `main` with `vX.Y.Z` to publish the image.
-->

## What & why

<!-- One or two lines: what this changes and the reason for it. -->

## Changes

-

## Verification

<!-- How you confirmed it works — checks/tests run, manual steps, screenshots for UI. -->

- [ ] `bun run check` passes
- [ ] `bun run test` passes
- [ ] Verified the change in the running app

## Notes

<!-- Anything else: follow-ups, deploy steps (new env vars, migrations), risks. Delete if none. -->
