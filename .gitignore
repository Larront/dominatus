node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
# SQLite
*.db*

# Uploaded scoresheet images — written next to the dev DB (repo root in dev, /data in prod)
/images

# Dev server output
dev-server.log

# Tesseract OCR language data, fetched at runtime to cwd
eng.traineddata
*.traineddata
